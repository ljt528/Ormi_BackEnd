'''
Tuple : 셀 수 있는 수량의 순서있는 열거

t = ()
t = (1, ) 값이 하나만 있어도 ,찍어주기

1. 값이 변하지 않는다. -> immutable
2. 시퀀스형 -> 순서 있다 -> 인덱스 -> 인덱싱, 슬라이싱 -> 인덱스 함수 사용 가능

Sequence 시퀀스형
문자형, 리스트, 튜플 -> 순서, 인덱스
1. in, not in
2. 같은 자료형끼리 연결 가능
list1 + list2 = [1, 2, 3, 4, 5]
3. 객체 반복 가능
list1 * 3 = [] [] []
4. 같은 요소의 갯수 확인 가능
.count("8")
'''